"use client"

import { useRouter } from "next/navigation"

const ShowMore = ({ pageNumber, isNext }) => {
    return (
        <div>
            ShowMore
        </div>
    )
}

export default ShowMore
